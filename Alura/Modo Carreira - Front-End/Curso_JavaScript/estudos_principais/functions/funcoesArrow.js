/*const soma = (num1, num2) => {
    return num1 + num2
}

console.log(soma(1, 2))*/

//ou mais simplificado:

const calculoDeSoma = (num1, num2) => num1 + num2 
const soma = calculoDeSoma(2, 5)
console.log(soma)